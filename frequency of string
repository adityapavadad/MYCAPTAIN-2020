def frequency(str):
    dict = {}
    for n in str:
        keys = dict.keys()
        if n in keys:
            dict[n] += 1
        else:
            dict[n] = 1
    return dict

print(frequency('mississippi'))

OUTPUT:-
{'m': 1, 'i': 4, 's': 4, 'p': 2}
